source env/core/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=core
export STEP_PACKAGENAME=json-glib
export PACKAGE_VERSION=1.10.8
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/core/json-glib/1.10.8/
rm -rf ${BUILD_DIRECTORY}/json-glib-1.10.8
tar xvf ${DOWNLOADDIR}/json-glib-1.10.8.tar.xz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/json-glib-1.10.8
	
	mkdir cross-build
	pushd cross-build
		meson ${MESON_CONFIG} -Dgtk_doc=disabled \
                      ..
		eval ${NINJA_AND_INSTALL}
	popd
	rm -f ${SYSROOT_DIR}/usr/lib${LIB_SUFF}/*.la
popd 
rm -rf ${BUILD_DIRECTORY}/json-glib-1.10.8
