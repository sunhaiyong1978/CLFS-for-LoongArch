source env/core/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=core
export STEP_PACKAGENAME=libxcrypt
export PACKAGE_VERSION=4.5.2
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/core/libxcrypt/4.5.2/
rm -rf ${BUILD_DIRECTORY}/libxcrypt-4.5.2
tar xvf ${DOWNLOADDIR}/libxcrypt-4.5.2.tar.xz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/libxcrypt-4.5.2
	patch -Np1 -i ${SYSDIR}/files/core/libxcrypt/4.5.2/patches/0001-fix-configure-error-under-loongarch64-architecture.patch 
	 
	for config_i in $(find -name "config.sub")
	do
		chmod 0644 $(dirname ${config_i})/config.{sub,guess}
		cp -a -f $(automake --print-libdir)/config.{sub,guess} $(dirname ${config_i})/
	done
	if [ ! -f configure ]; then
		autoreconf -ifv
	fi
	CC="${CROSS_TARGET}-gcc -fno-link-libatomic" ./configure ${COMMON_CONFIG} --disable-werror --enable-malloc0returnsnull
	make -j${JOBS}
	run_step_package_check check
	make DESTDIR=${SYSROOT_DIR} install
	rm -f ${SYSROOT_DIR}/usr/lib${LIB_SUFF}/*.la
popd 
rm -rf ${BUILD_DIRECTORY}/libxcrypt-4.5.2
