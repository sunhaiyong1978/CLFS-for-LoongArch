source env/core/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=core
export STEP_PACKAGENAME=inih
export PACKAGE_VERSION=r62
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/core/inih/r62/
rm -rf ${BUILD_DIRECTORY}/inih-r62
tar xvf ${DOWNLOADDIR}/inih-r62.tar.gz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/inih-r62
	
	mkdir cross-build
	pushd cross-build
		meson ${MESON_CONFIG}  \
                      ..
		eval ${NINJA_AND_INSTALL}
	popd
	rm -f ${SYSROOT_DIR}/usr/lib${LIB_SUFF}/*.la
popd 
rm -rf ${BUILD_DIRECTORY}/inih-r62
