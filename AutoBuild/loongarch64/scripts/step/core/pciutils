source env/core/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=core
export STEP_PACKAGENAME=pciutils
export PACKAGE_VERSION=3.15.0
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/core/pciutils/3.15.0/
rm -rf ${BUILD_DIRECTORY}/pciutils-3.15.0
tar xvf ${DOWNLOADDIR}/pciutils-3.15.0.tar.xz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/pciutils-3.15.0
	make -j${JOBS} CROSS_COMPILE="${CROSS_TARGET}-" HOST="${CROSS_TARGET}" \
	     PREFIX=/usr SHARED=yes LIBDIR=/usr/lib${LIB_SUFF}
	make CROSS_COMPILE="${CROSS_TARGET}-" HOST="${CROSS_TARGET}" \
	     PREFIX=/usr SHARED=yes LIBDIR=/usr/lib${LIB_SUFF} STRIP="" \
	     DESTDIR=${SYSROOT_DIR} install install-lib
popd 
rm -rf ${BUILD_DIRECTORY}/pciutils-3.15.0
