source env/base/config
source env/distro.info
source env/function.sh
export STEP_BUILDNAME=base
export STEP_PACKAGENAME=squashfs
export PACKAGE_VERSION=4.7.3
export RESOURCEDIR=${NEW_TARGET_SYSDIR}/files/base/squashfs/4.7.3/
rm -rf ${BUILD_DIRECTORY}/squashfs-tools-4.7.3
tar xvf ${DOWNLOADDIR}/squashfs4.7.3.tar.gz -C ${BUILD_DIRECTORY} 
pushd ${BUILD_DIRECTORY}/squashfs-tools-4.7.3
        pushd squashfs-tools
            XZ_SUPPORT=1 LZO_SUPPORT=1 LZMA_XZ_SUPPORT=1 LZ4_SUPPORT=1 ZSTD_SUPPORT=1 make -j${JOBS} CC="${CROSS_TARGET}-gcc"
            make CC="${CROSS_TARGET}-gcc" INSTALL_PREFIX=${SYSROOT_DIR}/usr \
                 INSTALL_MANPAGES_DIR=${SYSROOT_DIR}/usr/share/man/man1 install
        popd
popd 
rm -rf ${BUILD_DIRECTORY}/squashfs-tools-4.7.3
